import XCTest
@testable import ExpandableSideMenu

final class ExpandableSideMenuTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ExpandableSideMenu().text, "Hello, World!")
    }
}
